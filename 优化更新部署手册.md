# 不做清单 · 优化更新部署手册

> 每次优化产品时，从第 1 步开始按顺序执行，保证操作正确。

---

## 📋 每次优化的完整流程

### 第 1 步：开始优化前（备份当前状态）

打开命令行（`Win + R` → 输入 `cmd` → 回车），执行：

```bash
cd /d "h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev"
git status
```

**确认输出显示：**
```
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```

> ✅ 如果显示以上内容，说明当前是干净状态，可以开始优化。
> ⚠️ 如果显示有未提交的修改，先执行第 3、4 步提交当前修改，再开始新优化。

---

### 第 2 步：修改代码

在编辑器（如 Trae、VS Code）中修改代码，正常开发优化。

**建议：**
- 一次只优化一个功能，方便后面写提交说明
- 优化过程中随时在浏览器打开 `index.html` 预览效果
- 大改动前，可以先复制一份当前文件作为临时备份（改完删除）

---

### 第 3 步：本地测试

在浏览器中打开本地文件预览：

```
直接双击打开：h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev\不做清单demo\index.html
```

**测试清单：**
- [ ] 主页能正常显示
- [ ] 打印清单功能正常
- [ ] 时光机预测功能正常
- [ ] 票夹回顾功能正常
- [ ] 数据统计功能正常
- [ ] 手机端布局正常（用浏览器开发者工具模拟手机）

**确认没问题后，进入第 4 步。**

---

### 第 4 步：提交代码到 Git（记录版本）

打开命令行，执行：

```bash
cd /d "h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev"

# 1. 查看改了哪些文件
git status

# 2. 添加所有修改的文件
git add .

# 3. 提交并写明改了什么（把引号里换成实际内容）
git commit -m "7.26-修复清单滑动bug"
```

**提交信息命名规范：** `日期-改了什么`
- 示例：`7.26-修复手机端清单滑动bug`
- 示例：`7.27-优化统计页面图表样式`
- 示例：`7.28-新增暗黑模式`

---

### 第 5 步：推送到 GitHub（触发自动部署）

继续在命令行执行：

```bash
git push origin main
```

**成功标志：**
```
To https://github.com/0329-z/Stop-Doing-List.git
   8ff7e80..a1b2c3d  main -> main
```

> 如果弹出 GitHub 登录窗口，按提示登录即可。

---

### 第 6 步：等待 Cloudflare 自动部署

1. 推送后，Cloudflare 会**自动检测到代码更新**
2. 自动开始重新部署（不需要你操作）
3. 等待 **1-2 分钟**

**查看部署进度：**
1. 打开 https://dash.cloudflare.com/
2. 进入 Workers & Pages → 你的 Pages 项目
3. 点击 **Deployments** 标签
4. 看到最新部署显示绿色对勾 ✅ 表示完成

---

### 第 7 步：验证公网链接

打开你的公网访问链接（`xxx.pages.dev`），确认：
- [ ] 新功能/修改已生效
- [ ] 电脑端正常
- [ ] 手机端正常

> 💡 如果没看到更新，按 `Ctrl + F5` 强制刷新，或在链接后加 `?v=1` 访问。

---

### 第 8 步：文件夹备份（额外保险）

公网验证成功后，把当前版本复制一份到备份目录，作为额外保险。

**备份目录结构：**
```
h:\AI-projects\STOP-DOING LIST\web app\
├── stop-doing list dev\        ← 固定开发目录（一直在改）
└── backup\                     ← 备份目录
    ├── 7.24-初始版\            ← 每次优化后复制一份
    ├── 7.26-修复滑动bug\
    └── 7.28-优化统计图表\
```

**操作步骤：**

1. **创建 backup 文件夹**（第一次备份时才需要，以后跳过）
   ```bash
   mkdir "h:\AI-projects\STOP-DOING LIST\web app\backup"
   ```

2. **复制当前版本到 backup 目录**（用日期+改了什么命名）
   ```bash
   xcopy "h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev" "h:\AI-projects\STOP-DOING LIST\web app\backup\7.26-修复滑动bug\" /E /I /H /Y
   ```
   > 把 `7.26-修复滑动bug` 换成实际的日期和改动内容

**备份命名规范：** `日期-改了什么`
- 示例：`7.26-修复手机端清单滑动bug`
- 示例：`7.27-优化统计页面图表样式`
- 示例：`7.28-新增暗黑模式`

> 💡 文件夹备份是额外的安全感，Git 和 Cloudflare 已经有完整的历史记录，但多一份备份多一份安心。

---

## 🔙 回滚到历史版本（如果新版本有问题）

### 方式 A：Cloudflare 一键回滚（最简单，推荐）

1. 打开 https://dash.cloudflare.com/
2. 进入 Workers & Pages → 你的 Pages 项目
3. 点击 **Deployments**（部署）标签
4. 找到想回滚的历史版本
5. 点击右侧 **...** → **Rollback to this deployment**
6. 瞬间回滚完成！

### 方式 B：从文件夹备份恢复（直观，推荐）

1. 打开备份目录：`h:\AI-projects\STOP-DOING LIST\web app\backup\`
2. 找到想回滚的版本文件夹（如 `7.26-修复滑动bug`）
3. 把里面的所有文件复制，覆盖到开发目录：
   ```
   h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev\不做清单demo\
   ```
4. 然后执行推送命令，触发重新部署：
   ```bash
   cd /d "h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev"
   git add .
   git commit -m "回滚到7.26-修复滑动bug版本"
   git push origin main
   ```

### 方式 C：Git 命令回滚

```bash
cd /d "h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev"

# 查看所有历史版本
git log --oneline

# 回到指定版本（复制对应的版本号前7位）
git checkout a1b2c3d

# 如果确认要回到这个版本，强制推送
git push origin main --force
```

---

## ❓ 常见问题

### 问题 1：git push 失败，提示 "Permission denied"

**解决：**
1. 打开 https://github.com/settings/tokens
2. 点击 `Generate new token (classic)`
3. 勾选 `repo` 权限
4. 生成后复制 Token
5. 推送时密码处粘贴 Token

### 问题 2：git push 失败，提示 "rejected"

**原因：** GitHub 上的代码比本地新

**解决：**
```bash
git pull origin main
git push origin main
```

### 问题 3：公网链接没更新

**解决：**
1. 等待 2-3 分钟（部署需要时间）
2. 按 `Ctrl + F5` 强制刷新
3. 检查 Cloudflare 部署状态是否成功
4. 在链接后加 `?v=1` 访问

### 问题 4：本地测试正常，但公网有问题

**原因：** 缓存问题

**解决：**
- 等待几分钟后重试
- 清除浏览器缓存
- 用无痕/隐身模式打开

---

## 📁 重要路径速查

| 项目 | 路径 |
|------|------|
| **开发目录** | `h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev` |
| **网页文件** | `h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev\不做清单demo\` |
| **备份目录** | `h:\AI-projects\STOP-DOING LIST\web app\backup\` |
| **GitHub 仓库** | https://github.com/0329-z/Stop-Doing-List |
| **Cloudflare** | https://dash.cloudflare.com/ |
| **公网链接** | 你的 `xxx.pages.dev` 链接 |

---

## 📝 一键复制命令

每次优化完成后，依次执行这 4 条命令：

```bash
cd /d "h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev"
git add .
git commit -m "日期-改了什么"
git push origin main
xcopy "h:\AI-projects\STOP-DOING LIST\web app\stop-doing list dev" "h:\AI-projects\STOP-DOING LIST\web app\backup\日期-改了什么\" /E /I /H /Y
```

推送后等 1-2 分钟，公网自动更新。
最后一条命令会把当前版本备份到 backup 目录。完成！🎉
